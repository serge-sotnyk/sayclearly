Save the plan above as-is into a markdown file for review. Do not rewrite or restructure — copy the plan verbatim.

File naming: `docs/features/<N>_PLAN_<several_word_description>.md` where `<N>` is the next available feature number (starting with 0001), and `<several_word_description>` is a short snake_case summary of the feature.